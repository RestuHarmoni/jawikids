document.addEventListener('DOMContentLoaded',()=>{const p=JK.requireAuth();if(!p)return;renderNotifications();renderSupport();});
function renderNotifications(){const p=JK.currentParent(),s=JK.load(),el=document.querySelector('#notificationList');if(!el)return;const list=s.notifications.filter(n=>n.parent_id===p.id);el.innerHTML=list.map(n=>`<div class="card"><span class="badge">${n.type}</span><h3>${n.title}</h3><p>${n.message}</p><p class="muted">${new Date(n.created_at).toLocaleString('ms-MY')}</p></div>`).join('')||'<div class="card">Tiada makluman.</div>';s.notifications.forEach(n=>{if(n.parent_id===p.id)n.is_read=true});JK.save(s)}
function renderSupport(){const p=JK.currentParent(),s=JK.load(),el=document.querySelector('#ticketList');if(!el)return;const tickets=s.support_tickets.filter(t=>t.parent_id===p.id);el.innerHTML=tickets.map(t=>`<div class="card"><span class="badge ${t.status==='CLOSED'?'status-closed':'status-open'}">${t.status}</span><h3>${t.title}</h3><p class="muted">${t.category}</p><p>${t.message}</p></div>`).join('')||'<div class="card">Belum ada tiket support.</div>';const form=document.querySelector('#ticketForm');if(form&&!form.dataset.ready){form.dataset.ready=1;form.addEventListener('submit',e=>{e.preventDefault();const st=JK.load();st.support_tickets.unshift({id:JK.id('ticket'),parent_id:p.id,category:form.category.value,title:form.title.value,message:form.message.value,status:'OPEN',created_at:new Date().toISOString()});JK.save(st);form.reset();JK.toast('Tiket berjaya dihantar.');renderSupport();});}}
