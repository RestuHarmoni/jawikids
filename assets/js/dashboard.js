document.addEventListener('DOMContentLoaded',()=>{const p=JK.requireAuth();if(!p)return;const s=JK.load();const kids=s.children.filter(c=>c.parent_id===p.id);const noti=s.notifications.filter(n=>n.parent_id===p.id&&!n.is_read).length;const xp=kids.reduce((a,c)=>a+(c.xp||0),0);set('[data-child-count]',kids.length);set('[data-total-xp]',xp);set('[data-noti-count]',noti);set('[data-streak]','1 hari');const list=document.querySelector('#childSummary');if(list)list.innerHTML=kids.length?kids.map(c=>`<div class="card stat"><div class="icon">${c.avatar||'👦'}</div><div><b>${c.full_name}</b><div class="muted">Level ${c.level||1} • ${c.xp||0} XP</div><div class="progress"><span style="width:${Math.min((c.xp||0)%100,100)}%"></span></div></div></div>`).join(''):'<div class="card"><b>Belum ada profil anak</b><p class="muted">Tambah anak untuk mula belajar Jawi.</p><a class="btn" href="children.html">Tambah Anak</a></div>';});function set(q,v){const e=document.querySelector(q);if(e)e.textContent=v}
