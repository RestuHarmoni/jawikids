document.addEventListener('DOMContentLoaded',async()=>{const p=JK.requireAuth();if(!p)return;await JK.syncAll();renderAffiliate();});
function renderAffiliate(){const p=JK.currentParent();const s=JK.load();const existing=s.affiliate_interest.find(a=>a.parent_id===p.id);const status=document.querySelector('#affiliateStatus');if(status)status.innerHTML=existing?`<div class="card"><h3>Status: ${existing.status}</h3><p>Platform: ${existing.platform}</p><p class="muted">${existing.reason||''}</p></div>`:'<div class="card">Belum daftar minat affiliate.</div>';const form=document.querySelector('#affiliateForm');if(form&&!form.dataset.ready){form.dataset.ready=1;form.addEventListener('submit',async e=>{e.preventDefault();const parent=await JK.getSupabaseParent();const st=JK.load();const old=st.affiliate_interest.find(a=>a.parent_id===parent.id);if(old)return JK.toast('Anda sudah daftar minat affiliate.');const row={parent_id:parent.id,full_name:parent.full_name,email:parent.email,phone:parent.phone||'',platform:form.platform.value,reason:form.reason.value,status:'WAITING',created_at:new Date().toISOString()};const ins=await JK.insertRow('affiliate_interest',row);if(ins.error){console.error(ins.error);return JK.toast('Gagal daftar affiliate: '+ins.error.message)}st.affiliate_interest.push(ins.data||{...row,id:JK.id('aff')});JK.save(st);JK.toast('Minat affiliate berjaya dihantar.');renderAffiliate();});}}
